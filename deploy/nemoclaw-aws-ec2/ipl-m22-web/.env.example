# Local dev: point proxy to your EC2 GraphQL (HTTP is OK on localhost→server fetch)
IPL_GRAPHQL_BACKEND=http://13.223.70.88:8000/graphql

# On Vercel: Project → Settings → Environment Variables (same name, Production + Preview)
